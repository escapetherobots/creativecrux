<div class="container">
	<div class="jumbotron">
		<h1>My Projects Index Page</h1>
	</div>

	<% @projects.each do |p| %>
		<h2><%= link_to p.title, project_path(p) %></h2>
	<% end %>

	<%= link_to "Add Project", new_project_path %>
</div>